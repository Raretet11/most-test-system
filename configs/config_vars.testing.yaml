worker-threads: 4
worker-fs-threads: 2
logger-level: debug

is-testing: true

server-port: 8080

# my_database_db is the service name + _ + filename of the db.sql
dbconnection: 'postgresql://testsuite@localhost:15433/my_database_db'
